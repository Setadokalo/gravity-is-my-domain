# Gravity Is My Domain

This is just a small test project to implement a custom gravity influence system in Godot. While the system is currently implemented in 2D, it should fairly easily map to 3D as well.

Gravity is manipulated using subclasses of `GravityRegion`. The only subclass implemented in this demo is `GravitySphere`, but the logic is generic to any custom gravity shapes.
